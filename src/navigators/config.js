import {COLOR} from '../utils/colors';

export const tabScreenOptions = {
  tabBarActiveTintColor: COLOR.active,
  headerShown: false,
  tabBarStyle: {backgroundColor: COLOR.dark, minHeight: 90},
  tabBarLabelStyle: {fontSize: 12, fontWeight: '600'},
};
