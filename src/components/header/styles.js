import { StyleSheet } from 'react-native'
import { COLOR } from '../../utils'
export const styles = StyleSheet.create({
    container:{
    },
    title:{
        color:COLOR.white,
        fontWeight:'bold',
        fontSize:32
    }
})